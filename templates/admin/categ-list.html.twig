{% extends 'base-admin.html.twig' %}
{% block body %}
<h2>Tous les catégories ADMIN</h2>

<div class="b-example-divider"></div>
<div>
    <table class="table table-dark table-striped">
        <table class="table">

            <thead>
                <tr>
                    <th scope="col">#ID</th>
                    <th scope="col">Name</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                <tr>

                    {% for categorie in categorie %}
            <tbody>
                <tr>
                    <th>{{ categorie.id }}</th>
                    <td>{{ categorie.name }}</td>
                    <td><a href="{{ path('remove_categories', {id:categorie.id}) }}"><i class='bx bx-trash'></i></a>
                        <a href="{{ path('edit_categories', {'id': categorie.id, 'name': categorie.name}) }}"><i
                                class='bx bxs-edit-alt'></i></a>
                        <a href="{{path('add_categories') }}"><i class='bx bxs-plus-circle'></i></a>
                    </td>


                    {% endfor %}
                </tr>
            </tbody>
        </table>

        {% endblock %}